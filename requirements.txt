# Core dependencies for Person A's track (Data + GPU)
# Install on Windows/WSL2:  pip install -r requirements.txt
# For RAPIDS (cudf/cuml), use conda/miniforge inside WSL2 instead of pip.

# Data & ML
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
joblib>=1.3
matplotlib>=3.7
seaborn>=0.12

# Google Cloud (Day 2)
google-cloud-storage>=2.10
google-cloud-bigquery>=3.11

# GPU memory query (optional, for gpu_benchmark.py)
pynvml>=11.5

# RAPIDS (install via conda in WSL2, NOT pip):
# cudf, cuml — see RAPIDS Release Selector at https://rapids.ai/install

# Person B dependencies (Streamlit + Gemini)
streamlit>=1.30
google-genai>=1.0
python-dotenv>=1.0
